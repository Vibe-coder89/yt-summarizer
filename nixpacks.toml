[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install --break-system-packages -r requirements.txt"]

[phases.build]
cmds = []   # 🔥 THIS IS THE FIX (disable second install)

[phases.start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"